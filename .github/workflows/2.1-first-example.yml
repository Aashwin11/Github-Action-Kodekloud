name: 2.1 First Workflow #if not mentioned it will guve the relative path of the file
on: push

jobs:

    #name
    job_1:

        #where it runs
        runs-on: ubuntu-latest

        #steps
        steps:
            - name: Checkout repo
              uses: actions/checkout@v6


            #runs in series
            - name: Welcome Message | List and Read files
              run: |
                echo "1st flow"
                ls -ltra
                cat README.md

            - name: Generate ASCII Content
              run: cowsay -f dragon "Run for cover, Im a dragon" >>dragon.txts

            # - name: List files
            #   run: ls

            # - name: Read file
            #   run: cat README.md


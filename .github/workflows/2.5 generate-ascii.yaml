name: 2.5 First Workflow #if not mentioned it will guve the relative path of the file
on: push

jobs:

    #name
    job-1-Ascii-job: 

        runs-on: ubuntu-latest

        steps:

            - name: Checkout
              uses: actions/checkout@v6

            - name: Execute Shell
              run: |
               chmod +x 2.7-ascii-script.sh
               ./2.7-ascii-script.sh
